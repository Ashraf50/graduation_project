import 'package:flutter/material.dart';

class HosiptalListView extends StatelessWidget {
  const HosiptalListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
